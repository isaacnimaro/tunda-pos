// Sync is disabled in offline mode.
// These functions are kept as stubs to prevent breaking existing imports.

export async function syncData() {
  // No-op for offline mode
}

export function setupAutoSync() {
  // No-op for offline mode
}
